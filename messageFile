linearSystem_htd.py &
linearSystem_htd_TotFixedDT.py:

- reduced ERROR outpu
- convergent bifurcations: overshootTimes: the dummies (containing the edge indices) should not be floats but integers. Furthermore it is not necessary that they are
stored as arrays, now they are lists and integer output is guaranteed
